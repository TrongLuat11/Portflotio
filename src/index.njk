---
# Front matter — khai báo layout (giống @extends trong Blade)
layout: layouts/base.njk
---

{# 
  Trang chính (index.njk)
  Ghép tất cả các component lại thành one-page portfolio
  Giống file view chính trong Laravel, include các partial
  Mỗi section là 1 component .njk riêng biệt
#}

{# === Section 1: Hero — Giới thiệu đầu tiên === #}
{% include "components/hero.njk" %}

{# === Section 2: About Me — Giới thiệu bản thân === #}
{% include "components/about.njk" %}

{# === Section 3: Skills — Kỹ năng === #}
{% include "components/skills.njk" %}

{# === Section 3.5: Roadmap — Lộ trình học === #}
{% include "components/roadmap.njk" %}

{# === Section 4: Projects — Dự án nổi bật === #}
{% include "components/projects.njk" %}

{# === Section 5: Timeline — Kinh nghiệm & Học vấn === #}
{% include "components/timeline.njk" %}

{# === Section 6: Contact — Liên hệ === #}
{% include "components/contact.njk" %}
